# Create the vectors
animals <- c("Snake" , "Ostrich", "Cat", "Spider")
num_legs <- c("0","2","4","8")

print(animals)
print(num_legs)


# Create the data frame
animals_df <- data.frame(Animal = animals, NumLegs = num_legs)

# Print the data frame
print(animals_df)


# Option/Command/R to run all... alternatively can highlight all and Run.

